proc foo(a: int, b: int) int {
	return a - 3 + abs(b);
}

proc main() {
	x: int = 0x420;
	y: int = -1;

	big: int = 69 + (x / 2) << ( 3 * x );
	msg: string = "bye";

	if x > 0 {
		for x > 0 {
			x = foo(x - 1, y);
			print(x);
		}
	}
	else {
		print("Vai da nao");
	}

	print(msg);
}
