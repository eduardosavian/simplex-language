proc main() {
	b: int = 0b1010_1001;
	x: int = 0x420;
	y: int = -1;

	big: int = 69 + (x / 2) << ( 3 * x );
	msg: string = "bye";

	if (x > 0) == true {
		for x > 0 {
			x = foo(x - 1, y);
			print(x);
		}
	}
	else {
		print("Vai da nao");
	}

	print(msg);
}

proc foo(a: int, b: int) int {
	return a - 3 + abs(b);
}
